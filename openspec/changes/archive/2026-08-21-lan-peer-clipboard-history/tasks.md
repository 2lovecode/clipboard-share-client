## 1. 工程骨架与协议

- [x] 1.1 按 design 拆分 `protocol` / `history` / `clipboard` / `net` / `discovery` / `ui` 模块目录，并用 `cargo check` 确认工程可编译
- [x] 1.2 编写失败测试：protocol envelope（`auth` / `history_item` 等）编解码与非法帧拒绝，确认测试先红
- [x] 1.3 实现 protocol 编解码使 1.2 测试变绿，并用 `cargo test -p clipboard-share-client protocol`（或等价过滤）验证通过

## 2. 历史模型（clipboard-history）

- [x] 2.1 编写失败测试：显式 push 入队、剪贴板变化不入队、点击写回不触发出站消息、来源 local/remote 可区分
- [x] 2.2 实现内存历史列表（上限淘汰最旧）使 2.1 变绿，并用 `cargo test` 相关用例验证

## 3. 对等连接与口令（peer-connection）

- [x] 3.1 编写失败测试：口令正确握手成功、错误口令关闭、双连接按 peer_id 裁决只留一条
- [x] 3.2 实现 warp 监听 + async-tungstenite 拨号会话与口令握手，使握手相关测试变绿
- [x] 3.3 实现双连接裁决逻辑使碰撞测试变绿，并用 `cargo test` 验证
- [x] 3.4 实现手动 IP/端口连接路径的集成级测试或可脚本验证，确认可连上本机自环会话

## 4. mDNS 发现

- [x] 4.1 引入 mDNS 依赖并实现服务注册/浏览适配层；用单元或本地双进程冒烟确认能看到对端广告（发现失败时错误可观测）
- [x] 4.2 将发现结果接到 UI 可选列表，并验证「无发现时仍可手填 IP」的路径可用

## 5. 剪贴板载荷（clipboard-payload）

- [x] 5.1 编写失败测试：`ClipboardPort` 文本往返、HTML 可用时优先、不可用时回落纯文本、文件类型拒绝
- [x] 5.2 实现文本/HTML 读写适配使 5.1 相关用例变绿
- [x] 5.3 编写失败测试：图片载荷编码传输与写回成功/平台不支持时的明确失败
- [x] 5.4 实现图片载荷路径（含大小上限拒绝）使 5.3 变绿，并用 `cargo test` 验证

## 6. iced UI 与快捷键集成

- [x] 6.1 接入连接状态、口令/端口/显示名配置、发现列表与手动 IP 表单，手动跑通「配置 → 连接 → 状态显示」
- [x] 6.2 历史列表可点击写本机剪贴板，并用手动或测试确认点击不产生出站 `history_item`
- [x] 6.3 绑定显式推送热键（保留 Alt+C 语义）：推送时入队并在已连接时发送；断线时行为符合规格，用手动或测试验证

## 7. 端到端验证

- [x] 7.1 两实例（或本机双进程）验证：mDNS 或手填连接 + 口令 + 文本推送进双方历史 + 点击只写本机
- [x] 7.2 验证图片与 HTML 推送/点击写回（或平台不支持时的明确提示），并跑 `cargo test` 与 `cargo check` 全绿
